from wx import Colour

# can only create colors by RGB code, not by name, before wx.App is created
INPUT_BLUE = Colour(80, 80, 255)
LIGHT_GREY = Colour(200, 200, 200)
DARK_GREY = Colour(80, 80, 80)
ERROR_RED = Colour(255, 0, 0)