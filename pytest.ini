[pytest]
doctest_plus = enabled
addopts = --doctest-rst -q --tb=short -s
log_format = %(asctime)s %(levelname)s %(message)s
log_date_format = %Y-%m-%d %H:%M:%S
testpaths = testing
norecursedirs = src/wxfrog_example

